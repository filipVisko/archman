---
- name: Install LightDM
  community.general.pacman:
    name:
      - lightdm
      - lightdm-gtk-greeter
    state: present

- name: Enable LightDM
  ansible.builtin.service:
    name: lightdm.service
    enabled: true
